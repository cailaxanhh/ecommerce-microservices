# Consumer note: inventory-service includes a Kafka consumer (order-events + payment-events).
# In production on AWS, the consumer runs as a Lambda function (inventory-consumer)
# triggered by MSK event source mapping. The ECS/K8s deployment runs the full
# NestJS app including both the REST API and the in-process Kafka consumer.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: order-platform
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: order-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inventory-service
        app.kubernetes.io/component: service
        app.kubernetes.io/part-of: order-platform
    spec:
      containers:
        - name: inventory-service
          image: order-platform-inventory-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3005
              name: http
          envFrom:
            - configMapRef:
                name: order-platform-config
            - secretRef:
                name: order-platform-secrets
          env:
            - name: PORT
              value: "3005"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: order-platform
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: order-platform
spec:
  type: ClusterIP
  ports:
    - port: 3005
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: inventory-service
